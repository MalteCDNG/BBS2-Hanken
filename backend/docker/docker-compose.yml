services:
  mongo:
    image: mongo:4.4.18
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: YOURUSERNAME
      MONGO_INITDB_ROOT_PASSWORD: YOURPASS
    volumes:
      - ./db:/data/db
    ports:
      - "27017:27017"